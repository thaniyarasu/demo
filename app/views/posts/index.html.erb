<h1>Listing posts</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Title</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @posts.each do |post| %>
    <tr>
      <td><%= post.name %></td>
      <td><%= post.title %></td>
      <td><%= link_to 'Show', post %></td>
      <td><%= link_to 'Edit', edit_post_path(post) %></td>
      <td><%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } %></td>
    </tr>
    <% end %>
  </tbody>
</table>

<br />
<%= link_to 'New Post', new_post_path %>

--------------------------------------------------
<script  type="text/x-handlebars" >
  {{view Demo.MyTextField id="new-todo" placeholder="enter email"}}
  {{#collection contentBinding="Demo.todosController"}}
  {{view Em.Checkbox titleBinding="view.content.title" valueBinding="view.content.isDone"}}
  Hi {{view.content.title}}
  {{/collection}}


  {{#collection contentBinding="Demo.todosController" tageName="ul" itemClassBinding="content.isDone"}}
  {{view Em.Checkbox titleBinding="view.content.title" valueBinding="view.content.isDone"}}
  {{#if view.content.isDone}}
  {{view.content.title}}

  {{/if}}
  {{/collection}}

</script>

--------------------------------------------------
